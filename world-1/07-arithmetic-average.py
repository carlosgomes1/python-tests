# Make a program that reads two grades of a student and shows the average between them.

g1 = float(input('Enter the first grade: '))
g2 = float(input('Enter the second grade: '))

average = (g1 + g2) / 2

input(f'The average between {g1} and {g2} is {average}')
